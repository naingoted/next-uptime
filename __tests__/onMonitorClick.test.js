import "@testing-library/jest-dom/extend-expect";

describe("on clicking a service from monitor component", () => {
  test("showing quick stats on click", () => {
    // trigger
    expect(1).toBe(1);
  });
});
